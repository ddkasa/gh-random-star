[tool.poetry]
name = "github-random-star"
version = "0.1.0"
description = ""
authors = ["David Kasakaitis <davidkasakaitis@proton.me>"]
readme = "README.md"

[tool.poetry.scripts]
cli = "github_random_star:main"

[tool.poetry.dependencies]
python = "^3.12"
httpx = "^0.26.0"
fire = "^0.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
